We are using the same branch for our cp1 regrade and cp3 grade, so make sure to 
run the fib test before looking at cp1 regrades. 

Nothing works except for fib, so you can just run make-kernel in the master branch.

Reference counts have been implemented. Locking has also been implemented.

